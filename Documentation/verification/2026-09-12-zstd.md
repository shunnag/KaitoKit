# Zstandard 検証記録（2026-09-12）

対象: `feat/zstd`、bd `cooViewer-c1vj.3`。作業ツリーで実装・検証した。
checkout / restore / stash / reset / commit / push は行っていない。

## 出自 / Provenance

初回実装の参照資料は利用者提供のローカル仕様二点と既存 KaitoKit のソースのみ。
同梱 `inbox/zstd/SHA256SUMS` と実ファイルの SHA-256 が一致した。

| 仕様 | SHA-256 |
|---|---|
| `inbox/zstd/rfc8878.txt` — RFC 8878 | `8ee6be03534113f5689cda75b9539a02e0704a2506d420814223e506420aeea4` |
| `inbox/zstd/xxhash_spec.md` — XXH64 algorithm description | `61c47af338b2feefcc7621700ef6b941bcf9b1dd33899c503f68cb797e7ef333` |

Web は閲覧していない。facebook/zstd、7-Zip / p7zip、libarchive、XADMaster、
The Unarchiver、zstd-rs、zstandard、他言語移植、教育用実装、ブログの解説コードは
開かず、検索・参照していない。FSE の定義済み分布は RFC §3.1.1.3.2.2 の数値から構築した。
zstd CLI と 7zz はプロジェクト所有データの圧縮・展開を照合するブラックボックスとしてだけ実行した。

> Only the two supplied specifications and existing KaitoKit sources were consulted. Both document
> hashes matched. No web pages, reference implementations, ports or tutorial code were accessed.
> zstd and 7zz were used exclusively as black-box fixture tools and extraction oracles.

## 実装範囲 / Implementation

`Sources/KaitoKit/Codecs/Zstd/` の六ファイルは、前向き入力と逆向きビット読取り、FSE 分布・
状態表、Huffman direct / FSE weights と 1 / 4 stream、literals / sequences、frame と XXH64 を扱う。
LL / ML / OF の accuracy log 上限は 9 / 9 / 8、Huffman weights の FSE は 6、Huffman 符号は 11 bit。
offset code は 0〜31。辞書 ID が非零なら `unsupportedMethod("zstd dictionary <id>")`。

ByteSource の指定範囲からヘッダを最大 4 KiB 先読みし、block 単位で出力する。
本文は先読み済みの残りを消費し、不足分が 4 KiB 以上なら直接まとめて読む。
履歴は空から実出力に応じて伸ばし、保持上限に達したらリングとして更新する。
保持上限は既知サイズなら `min(windowSize, max(1, contentSize))`、不明なら宣言 window。
block / literals の作業領域は各最大 128 KiB。
宣言 window は従来どおり `ReadLimits.maxDictionarySize`（既定 1 GiB）で確認する。
重なる match は直前に出力した byte を参照し、履歴と window を越える距離を拒否する。
frame が変わると Huffman / FSE table・repeat offset・checksum をリセットする。

連結 frame と skippable frame は一つの連続出力になる。全 frame が content size を持てば
一覧の uncompressedSize に合計を保存し、一つでも欠ければ nil。宣言サイズと実出力も検証する。
`.tar.zst` / `.tzst` は既存の SingleFileMaterializer / TarReader を通る。
RPM は payload magic を優先して cpio に降り、ZIP method 93 は中央 directory の期待サイズと CRC も検証する。
通常の entry / 総量 / in-memory 上限と reopen を既存 API で維持する。

> Six codec files implement FSE/Huffman, literals/sequences, frame processing and streaming XXH64.
> Input and history are bounded, overlapping copies preserve byte order, and state resets between frames.
> Standalone/tar streams, RPM cpio payloads and ZIP 93 share the decoder and existing resource limits.
> External dictionaries and zstd inside 7z remain unsupported.

## 固定 fixture / Fixed corpus

生成器: [make-zstd.py](../../Scripts/fixtures/make-zstd.py)。Python 標準ライブラリと zstd CLI を使用する。
固定 fixture は 46 件。各 `.b64` は最大 22,152 byte（40,000 byte 未満）。
元入力の名前・サイズ・SHA-256、圧縮オプション、観測した block 機能は
[manifest.json](../../Tests/Fixtures/zstd/manifest.json) に記録した。

| 件数 | 入力と条件 |
|---:|---|
| 30 | text / structured binary / uniform random / repetitive / empty / one byte × level 1 / 3 / 9 / 19 / ultra 22、single-thread、checksum |
| 7 | no-check、no-content-size、T4、rsyncable、long=20（1 MiB window）、zero RLE blocks、512 KiB の table 再利用 |
| 4 | 連結、サイズ不明を含む連結、先頭・中間・末尾の skippable、skippable のみ |
| 1 | `zstd --train --dictID=8878` の辞書で圧縮した拒否用 frame |
| 1 | 決定的 USTAR を Python tarfile で作った `.tar.zst` |
| 2 | 最小 ZIP method 93 envelope、通常 frame と skippable を挟む連結 frame |
| 1 | 既存 project-owned stored RPM の envelope / cpio に zstd payload を組み込んだもの |

`table-reuse.zst` では辞書を使わず direct / FSE weights、4 stream、Treeless literals、Repeat FSE
が複数 block に現れることを生成器で検査する。辞書付き拒否標本を coverage に数えない。
正常な `.zst` 42 件と ZIP 2 件は生成時にも 7zz で照合し、`.zst` は zstd でも照合する。
期待ハッシュは展開結果から逆算せず、圧縮前の元データから計算する。
別ディレクトリに再生成した 47 ファイル（manifest を含む）が全て byte 一致した。
既存 `container/rpm-zstd.rpm` も gzip 等と同じ六つの cpio entry と SHA-256 になることを確認する。

> The 46 fixtures cover all listed compression options and containers. Every base64 file is below
> 40 KB. Dictionary-free table reuse is checked structurally. Forty-four valid streams/ZIPs are
> compared with 7zz, with expected hashes computed before compression. All 47 generated files,
> including the manifest, reproduce identically.

## 差分と破損入力 / Differential and malformed-input tests

実行時 matrix は 5 入力 × 4 level × 2 checksum × 2 content-size = **80 通り**。
text 200 KiB、binary 256 KiB、random 64 KiB、repetitive 1 MiB、mixed 512 KiB の元入力と byte 比較する。
zstd がなければこのテストのみ XCTSkip、7zz がなければ第二オラクルのテストのみ XCTSkip とする。

`ZstdTests` は固定全件の一覧・SHA-256・reopen・127-byte stream と、全 44 正常標本の第二オラクルを検査する。
46 件それぞれに 25 / 50 / 90% / 最後の 1 byte の切断（184 件）、24 箇所の bit 反転（1,104 件）を適用する。
frame / sequence の Reserved bits、block-size / window 超過、Huffman の不正重み合計、FSE の分布合計超過、
履歴超過、repeat offset が零になるケース、宣言サイズ不一致、checksum、Dictionary_ID を直接構築して検証する。
FSE 破損は checksum だけに頼らず table parser / builder でも確認する。

仕様から手作りした正常ケースには、direct weights、最大 11 bit の Huffman、1 / 4 stream、RLE literals、
RLE / Repeat sequence table、1 / 2 / 3-byte sequence count、全 skippable magic、全 content-size 幅、
window descriptor の mantissa、リング折り返し、window と等しい offset、repeat offset の -1 特例を含む。
7-byte の短い ByteSource read、先読みから直接読取りにまたがる raw block、範囲外アクセス禁止、terminal error も検査する。

bit 反転は必ずしも破損を意味しない。RFC の Unused bit（bit 4）は無視する必要があり、
skippable 本文や checksum 無しの合法な内容変更も受理できる。
これらまで一律拒否するのではなく、切断と構造的に不正なケースは KaitoError、任意反転は
正常終了または KaitoError に限られることを確認する。crash / hang は許容しない。

> The runtime matrix has 80 cases and compares exact original bytes. Tests also cover 184 truncations,
> 1,104 bit flips, malformed entropy tables, lengths, offsets, checksums, dictionary IDs and resource limits.
> Hand-built valid cases exercise boundary encodings and ring wrap. Arbitrary flips may remain valid
> in unused bits, skippable metadata or unchecked content; those are not falsely required to fail.

追加の black-box probe では、重み `[2, 2]`（最後の重みは 3）を持ち、重み 1 の葉がない
非正規の Huffman 表に対し、zstd は corruption を返し、7zz は `00 01 02` を返した。
本実装は、導出された木の深度と最長符号長の一致、および最深段の兄弟の葉を要求して拒否する。
この差は第二オラクルの寛容な入力受理として記録し、正常 fixture の一致とは区別する。

> One malformed-tree probe differed between oracles: weights [2, 2] imply no deepest-rank leaves.
> zstd rejects it while 7zz decodes it. KaitoKit requires a consistent tree depth and paired deepest
> leaves, and rejects this noncanonical representation. Valid-fixture comparisons remain identical.

## 検証環境と再実行 / Environment and reproduction

- macOS arm64、Apple Swift 6.4（swiftlang-6.4.0.34.1）、Swift 6 language mode。
- `/opt/homebrew/bin/zstd` 1.5.7、`/opt/homebrew/bin/7zz` 26.03 arm64。
- managed sandbox の書込み範囲に合わせ、SwiftPM / Clang の cache は `/tmp` に指定した。
  通常の home cache への書込み拒否はソースのコンパイル失敗と区別する。
- release の dSYM 生成は sandbox に拒否されたため、性能測定は `--product kaito -debug-info-format none` でビルドする。

```sh
python3 Scripts/fixtures/make-zstd.py --help
python3 Scripts/fixtures/make-zstd.py --output /tmp/kaitokit-zstd-regenerated

export CLANG_MODULE_CACHE_PATH=/tmp/kaitokit-zstd-module-cache
swift build --disable-sandbox --cache-path /tmp/kaitokit-swift-cache \
  --config-path /tmp/kaitokit-swift-config --security-path /tmp/kaitokit-swift-security
swift test --disable-sandbox --cache-path /tmp/kaitokit-swift-cache \
  --config-path /tmp/kaitokit-swift-config --security-path /tmp/kaitokit-swift-security \
  --filter 'ZstdTests|SingleFile|Rpm|ZipHardening'
swift test --disable-sandbox --cache-path /tmp/kaitokit-swift-cache \
  --config-path /tmp/kaitokit-swift-config --security-path /tmp/kaitokit-swift-security
swift build -c release --product kaito -debug-info-format none --disable-sandbox \
  --cache-path /tmp/kaitokit-swift-cache --config-path /tmp/kaitokit-swift-config \
  --security-path /tmp/kaitokit-swift-security
git diff --check
git status --short
```

> Caches are redirected to writable temporary paths in the managed sandbox. The release CLI benchmark
> omits debug symbols because dSYM generation was denied by that sandbox. No runtime dependency is added.

## テスト結果 / Test results

最終コードの対象フィルタ `ZstdTests|SingleFile|Rpm|ZipHardening` は KaitoKit 104 件、
Compat 2 件、合計 **106 件・失敗 0・skip 0**。
このうち ZstdTests は 12 件。80 通りの生成 matrix は圧縮生成を含め **5.309 秒**で全入力と一致した。
正常 fixture の 7zz 比較 44 件も全一致。切断・反転・構造的な不正入力で crash / hang はなかった。

> The final targeted run passed all 106 tests without skips. Twelve ZstdTests include the 80-case
> generation matrix, completing in 5.309 seconds including compression, and 44 matching 7zz comparisons.
> Malformed-input testing completed without crashes or hangs.

続いて最終コードの全 bundle と `swift build` を再実行した。
debug build と release CLI build はともに終了 0、コンパイラ warning / error は 0。

| 最終全件実行 | 件数 | skip | 失敗 | 時間 |
|---|---:|---:|---:|---:|
| KaitoKitTests | 894 | 38 | 0 | 276.233 s |
| KaitoKitCompatTests | 23 | 0 | 0 | 0.693 s |
| 合計 | **917** | **38** | **0** | |

全件実行中の ZstdTests は 12 件・失敗 0（71.720 秒）、matrix は **5.162 秒**。
38 skip は既存の任意 corpus / ツール条件によるもの。新規 ZstdTests の skip は 0。
ログの `zip error: ... bzip2 not enabled` は既存 Info-ZIP 差分テストが能力不足を検出した
skip の診断で、テスト失敗ではない。`git diff --check` と生成器 `--help` も終了 0。

> Final verification passed 917 tests across both bundles with zero failures and 38 existing optional
> skips; none of the ZstdTests were skipped. The final matrix took 5.162 seconds. Debug and release
> CLI builds completed without compiler warnings or errors. Diff whitespace and generator help checks passed.

## 性能 / Performance

最終 release CLI、同一の決定的テキスト 10 MiB（10,485,760 byte）を zstd level 3 で圧縮。
圧縮後は 937,379 byte。15 回の中央値を比較した。

| 測定 | 中央値 |
|---|---:|
| KaitoKit open | 0.106 ms |
| KaitoKit readAll（XXH64 検証・Data 確保を含む） | 32.835 ms |
| zstd CLI `-q -d -c` → `/dev/null`（プロセス起動を含む） | 8.152 ms |

展開は 0.5 秒の目安以内、CLI に対して約 4.0 倍。
リング履歴への追記は境界で最大二つの連続コピーに分け、byte ごとのリング更新を避ける。
展開後 SHA-256 は元入力と一致:
`f94d31471082c56b0457f594722c8501f6291dbf48f0e9d226b92f37c289cef6`。

```sh
python3 - <<'PY'
from pathlib import Path
import runpy, subprocess
generator = runpy.run_path('Scripts/fixtures/make-zstd.py')
directory = Path('/tmp/kaitokit-zstd-bench')
directory.mkdir(exist_ok=True)
source = directory / 'text'
source.write_bytes(generator['payload']('text', 10 * 1024 * 1024))
subprocess.run(['/opt/homebrew/bin/zstd', '-q', '-f', '-3', str(source),
                '-o', str(source) + '.zst'], check=True)
PY
.build/release/kaito bench /tmp/kaitokit-zstd-bench/text.zst 15
```

> The final release decoder reads and verifies 10 MiB in 32.835 ms (median of 15), versus 8.152 ms
> for zstd CLI including process startup. This meets the 0.5-second and 5× CLI targets on this machine.
> The decoded SHA-256 matches the original input. Results include Data allocation and checksum verification.

## オーケストレータ側の独立検証(2026-09-12)

Codex の検証とは別に、orchestrator が release / ASan の `kaito` CLI で確認した。

| 検証 | 結果 |
| --- | --- |
| zstd CLI 1.5.7 との差分(release、seed 20260912 / 777) | 240 ケース、不一致 0。level 1〜19 と `--ultra -22`、`--long=20/24`、`--no-check`、`-C`、`--no-content-size`、`--single-thread`、`-T4`、`--zstd=wlog=16`、`--zstd=tlen=2`、`-B64K` を乱択。入力は Swift source・RFC 本文・PNG・一様乱数 64 KiB / 2 MiB・高反復 1 MiB・0 埋め 4 MiB・RLE 的・混合・空・1 byte |
| 連結 frame と skippable frame | 上記のうち約 3 割で `skip + frame + skip + frame + skip` を合成し、連結出力と一致 |
| `.tar.zst` | 上記のうち約 2 割で tar を生成し、member ごとの SHA-256 が一致 |
| 同 差分(ASan 版、seed 31337) | 40 ケース、不一致 0、ASan 報告 0 |
| hostile 入力(ASan 版 `kaito sha`) | 12,831 通り(固定 fixture + 3 level の生成物 × 切断 5 / bit 反転 48 / frame header descriptor 全 256 値 / header byte 乱択 16 / skippable・二重 magic 4)、crash・ASan 報告・timeout 0 |
| 性能(release、Swift source 連結 19.7 MB、level 3) | `kaito sha` 0.10 秒 ≈ 197 MB/s(zstd CLI は 0.01 秒) |

> Independent orchestrator checks: 240 randomly parameterized zstd CLI archives (levels 1-22 with long
> mode, checksum and content-size variants, multi-threading, concatenated and skippable frames and
> .tar.zst) matched byte for byte, 40 more under AddressSanitizer, and 12,831 hostile mutations
> produced no crash or sanitizer report. Throughput is about 197 MB/s, roughly 10x slower than the
> reference CLI; optimization is tracked separately.

## adversarial review 後の是正（2026-09-12）

指摘は計 7 件で、確認 3 件・却下 4 件。確認された指摘のうち 2 件は同じ window 即時確保を原因とする DoS、
残る 1 件は一覧取得時の先読み破棄による不要な読取りだった。次の変更で解消した。
是正前の DoS の時間は利用者提供の独立レビュー結果、一覧の読取り量は今回の修正前後で実測した値である。

| 確認された指摘 | 是正前 | 是正後 |
|---|---|---|
| サイズ不明・1 GiB window の空フレーム連結 | `28b52ffd00a0010000` × 10,000（90,000 バイト）で 1 フレームごとに 1 GiB をゼロ確保。user CPU 約 45 秒、出力 0、終了 0 | 履歴は毎回 0 バイト。debug CLI の実時間 0.035423 秒、出力 0、終了 0 |
| 単一セグメントの巨大な既知サイズ宣言 | `28b52ffde0` + 8 バイトの 1 GiB 宣言 + `010000`（16 バイト）で即時確保が発生 | 履歴は初期化時・拒否時とも 0 バイト。実時間 0.009958 秒、従来の `zstd frame content size mismatch`、終了 1 |
| `contentSize` によるブロック走査 | 128 KiB の raw block × 16、入力 2,097,213 バイトに対し 1,048,576 バイトを読取り | 65,536 バイトに低減（1/16、93.75% 減）。読取り回数はどちらも 16 回 |

時間はプロセス起動を含む `kaito sha` の一回の観測値で、合否条件には使わない。
`allocatedWindowBytes` は `window.count` を返す内部アクセサで、RSS や配列の予約容量ではない。
回帰テストは初期値 0 と各ブロック後の `window.count <= max(1, produced)` を検査する。
空フレーム 10,000 件は各 decoder と実際の連結復号経路の両方を通す。
16 バイト入力では確保を行わず、正確なサイズ不一致エラーを確認する。

### 却下した 4 件

以下は利用者提供の adversarial review の検証結果であり、却下対象の実装と既存テストは変更していない。

| 指摘箇所 | 指摘と却下理由 |
|---|---|
| `ZstdDecompressor.swift:55` | ZIP method 93 の復旧が同じ壊れ方の deflate entry より厳しいという指摘。機構は実在するが、結果は契約で許容される `KaitoError.malformed` であり、当該行の magic 検査は形式上必須。実際の分岐は `ZipReader.swift` の recovery marker 探索で、指摘の行への紐付けも誤っているため却下。 |
| `SingleFileReader.swift:63` | 余剰の末尾バイトがある `.zst` を一覧取得できないという指摘。再現するが、非適合ストリームに対する契約どおりの結果。他実装なら読めるという oracle の根拠は実測で誤りであり、厳密な扱いも既存 codec と同じ既定方針のため却下。 |
| `RpmReader.swift:40` | zstd 対応により blob entry への縮退がなくなり、fallback の assertion も削除されたという指摘。文書化済みの意図した挙動で、`KaitoError.unsupportedMethod` は契約に準拠する。該当 assertion は存在し、削除されたという主張自体も事実誤認のため却下。 |
| `Tests/KaitoKitTests/RpmReaderTests.swift:233` | corrupt-payload テストで zstd の反転位置が trailer 外になり、終端検証を行っていないという指摘。バイト位置の指摘は正しいが、反転後も終端検証が発火することを実測済み。「検証が働かない」という結論が偽のため却下。 |

### 保持量と受理条件

LZMADecoder の `retainedDictionarySize` と同じく、出力全体より古いバイトは参照できない。
`retainedWindowSize` を既知サイズで制限しても、宣言 `windowSize` は block 上限、match 距離、
`maxDictionarySize` の検査にそのまま使う。新たな 128 MiB 制限は導入していない。
保持上限まで append し、残りだけを既存の最大二回のリングコピーで追記する。
既知サイズの超過検査は履歴を伸ばす前に行い、最終ブロックは従来どおり履歴へ保存しない。

既に正常に出力した量を P、宣言 window を W、保持上限を R とする。
不明サイズでは R = W。既知サイズ S では P <= S が保証されるため、
`min(R, P) == min(W, P)` であり、`historyCount` は是正前と等しい。
旧 `window.count` は常に W だったので、`offset <= header.windowSize` と
`offset <= historyCount + output.count` は、旧検査と同じ距離の集合を受理・拒否する。
負の参照位置では `-source <= historyCount <= window.count`。満杯前は
`windowPosition == window.count`、満杯後は `0 <= windowPosition < window.count` なので、
実保持量で一回折り返すだけで配列の範囲に収まる。

追加テストは既知サイズ 0 / 1 / 4 / 4,097、巨大宣言に対する従来の辞書上限、
出力サイズ超過時の無確保、空履歴でのブロック内 match、履歴とリテラルをまたぐ match、
保持上限の直前・一致・追記途中での到達、複数周のリング、既知・不明サイズを検査する。
既存の window 距離と等しい offset の成功・1 バイト超過の拒否も変更していない。

この worktree には `inbox/zstd/` が存在しなかったため、今回の根拠確認には
[RFC Editor 公開版の RFC 8878](https://www.rfc-editor.org/rfc/rfc8878.html#section-3.1.1.3)
と既存の LZMA / zstd 実装を用いた。初回実装の出自記録とはこの点が異なる。
参照実装のソースは開いていない。zstd CLI は入力と出力の照合だけに使用した。
RFC の必要履歴は宣言 window とフレーム開始までの距離の小さい方であり、この範囲を保持する。
ヘッダ受理条件や復号バイト列を変えず、保持領域の確保時期だけを遅らせる。
なお、RFC §3.1.1.1.2 の Window_Descriptor 全範囲の最大値は 3.75 TiB である。
依頼文の最大値の単位に差があるが、今回の再現値 `0xa0 = 1 GiB` と修正には影響しない。

### 読取り量の計測

`CountingSource` で ByteSource の実読取り量と回数を記録した。
修正前に `testListingReadVolumeAndDirectBlockReads` だけを実行し、
`1048576 > 65536` により想定どおり失敗することを確認した。修正後は同じテストが通る。

| 128 KiB × 16 ブロック | 修正前のバイト数 / 回数 | 修正後のバイト数 / 回数 |
|---|---:|---:|
| 一覧の構造走査 | 1,048,576 / 16 | 65,536 / 16 |
| 全ブロック復号 | 2,097,213 / 33 | 2,097,213 / 32 |

復号の先読み残量を消費してから直接読むことで、余分な再読取りを発生させない。
4 KiB 境界の前後（4,095 / 4,096 / 4,097 バイト）、buffer 内・境界一致・buffer 外の skip、
空読取り・負の長さ・範囲超過・末尾も追加テストで確認した。`skip` の実装は変更していない。
計測用 raw フレームの出力 2,097,152 バイトは zstd CLI の出力と一致し、SHA-256 は
`c1b153e61d7d7835c625cc3077b85c18808a8c0f6e6b157b9bfc4546b5a34abb` だった。

### 是正後の初回検証

初回の是正検証では、キャッシュと計測用標本をこの worktree の `.build/zstd-verification/` に配置した。
ビルド出力は `.build/` を使用した。
ビルド時の一時領域も同ディレクトリの `tmp/` に指定し、別 worktree は変更していない。
以下は対象 worktree のルートで実行する。

```sh
mkdir -p .build/zstd-verification/{tmp,module-cache,swift-cache,swift-config,swift-security}
export TMPDIR="$PWD/.build/zstd-verification/tmp"
export CLANG_MODULE_CACHE_PATH="$PWD/.build/zstd-verification/module-cache"
swift_options=(--disable-sandbox
  --cache-path .build/zstd-verification/swift-cache
  --config-path .build/zstd-verification/swift-config
  --security-path .build/zstd-verification/swift-security)
swift build "${swift_options[@]}"
swift test "${swift_options[@]}"
swift run "${swift_options[@]}" kaito list .build/zstd-verification/listing-128k.zst
swift run "${swift_options[@]}" kaito list .build/zstd-verification/text-l3.zst
.build/debug/kaito sha .build/zstd-verification/empty-windows.zst
.build/debug/kaito sha .build/zstd-verification/single-huge-size.zst
git diff --check
```

初回の追加 5 テストの個別実行は **5 件・失敗 0・skip 0、0.062 秒**。
`swift build` は `Build complete! (0.23秒)`、終了 0。
`swift run kaito list` の出力は以下で、いずれも終了 0 だった。

```text
0	2097152	file	Zstandard	plain	listing-128k
0	49152	file	Zstandard	plain	text-l3
```

### 専用ビルド先での最終検証

並行して動いていた別の SwiftPM との競合を避け、最終検証は同じ worktree 内の
`.build/zstd-fix-check/` にビルド・キャッシュ・一時領域・ログを分離した。
実装の受理条件・既存の期待出力・却下した指摘のコードは変更していない。
追加テストでは、保持上限より大きい圧縮ブロックの符号化サイズ、既知サイズでの
履歴とリテラルをまたぐ参照、到達可能な履歴があっても宣言 window を超える距離の拒否も確認した。

最初の全件実行では 922 件中、既存の 3 テストで計 7 個の assertion が失敗した。
全て `FileByteSourcePermissionTests` と `RARCommonPrimitiveTests` における
`file:///private/tmp/...` と `file:///tmp/...` の URL 文字列表記の相違だった。
この実行でも zstd 17 件は失敗 0。実装やテストの変更はせず、TMPDIR を同じ実ディレクトリの
`/tmp` 表記に揃えると、該当する両 suite の **19 件が失敗 0・skip 0（0.371 秒）**となった。
この環境で全件を再実行し、以下の結果を得た。別 worktree や worktree 外の一時領域は使用していない。

```sh
mkdir -p .build/zstd-fix-check/{tmp,module-cache,swift-cache,swift-config,swift-security}
export TMPDIR="${PWD#/private}/.build/zstd-fix-check/tmp"
export CLANG_MODULE_CACHE_PATH="$PWD/.build/zstd-fix-check/module-cache"
swift_options=(--scratch-path .build/zstd-fix-check/build --disable-sandbox
  --cache-path "$PWD/.build/zstd-fix-check/swift-cache"
  --config-path "$PWD/.build/zstd-fix-check/swift-config"
  --security-path "$PWD/.build/zstd-fix-check/swift-security")

python3 - <<'PY'
from pathlib import Path
import base64, struct
directory = Path('.build/zstd-fix-check')
body = bytes((i * 7 + 3) & 255 for i in range(128 * 1024))
listing = bytes.fromhex('28b52ffde0') + struct.pack('<Q', len(body) * 16)
listing += b''.join(((len(body) << 3) | (i == 15)).to_bytes(3, 'little') + body for i in range(16))
(directory / 'listing-128k.zst').write_bytes(listing)
(directory / 'text-l3.zst').write_bytes(base64.b64decode(Path('Tests/Fixtures/zstd/text-l3.zst.b64').read_bytes()))
(directory / 'empty-windows.zst').write_bytes(bytes.fromhex('28b52ffd00a0010000') * 10000)
(directory / 'single-huge-size.zst').write_bytes(bytes.fromhex('28b52ffde0') + struct.pack('<Q', 1 << 30) + bytes.fromhex('010000'))
PY

swift build "${swift_options[@]}"
swift test "${swift_options[@]}" --filter 'FileByteSourcePermissionTests|RARCommonPrimitiveTests'
swift run "${swift_options[@]}" kaito list .build/zstd-fix-check/listing-128k.zst
swift run "${swift_options[@]}" kaito list .build/zstd-fix-check/text-l3.zst
swift test "${swift_options[@]}"
.build/zstd-fix-check/build/debug/kaito sha .build/zstd-fix-check/empty-windows.zst
.build/zstd-fix-check/build/debug/kaito sha .build/zstd-fix-check/single-huge-size.zst
/opt/homebrew/bin/zstd -q -d -c --memory=1024MB .build/zstd-fix-check/empty-windows.zst > /dev/null
git diff --check
```

| 最終全件実行 | 件数 | skip | 失敗 | 時間 |
|---|---:|---:|---:|---:|
| KaitoKitTests | 899 | 38 | 0 | 274.091 秒 |
| KaitoKitCompatTests | 23 | 0 | 0 | 0.760 秒 |
| 合計 | **922** | **38** | **0** | |

`swift test` は終了 0。zstd は **17 件・失敗 0・skip 0（71.150 秒）**で、
既存 12 テストの内容は維持した。80 ケースの生成 matrix は **5.255 秒**で全入力と一致し、
固定 46 fixture の一覧・SHA-256・reopen・小分け読取りと、正常 44 標本の 7zz 比較も成功した。
38 skip は既存の任意 corpus / ツール条件で、Info-ZIP の bzip2 非対応による診断もその一つである。
追加 5 テストの個別実行は **5 件・失敗 0・skip 0（0.067 秒）**。

代表的なコマンド出力は以下のとおり。ビルドと一覧取得は全て終了 0。

```text
swift build: Build complete! (0.37秒)
swift run の一覧取得:
0	2097152	file	Zstandard	plain	listing-128k
0	49152	file	Zstandard	plain	text-l3
swift test:
Executed 899 tests, with 38 tests skipped and 0 failures (0 unexpected) in 274.091 (274.147) seconds
Executed 23 tests, with 0 failures (0 unexpected) in 0.760 (0.762) seconds
zstd 巨大 window: 空フレーム 10000 件と 16 バイト単一フレームの履歴確保は各 0 バイト
zstd 一覧の読取り: 入力 2097213 バイト、読取り 65536 バイト、16 回
zstd 復号の読取り: 2097213 バイト、32 回
```

攻撃標本は debug CLI を各 5 回実行し、プロセス起動を含めた実時間も記録した。
合否は引き続き内部アクセサの確保量で判定し、時間や RSS を assertion には使わない。

| 標本 | 実時間の中央値 | 5 回の最大値 | 出力・終了状態 |
|---|---:|---:|---|
| 9 バイト × 10,000 | 0.035220 秒 | 0.311666 秒 | 出力 0 バイト、終了 0 |
| 16 バイトの 1 GiB 宣言 | 0.008085 秒 | 0.008234 秒 | 従来の content size mismatch、終了 1 |

16 バイト標本の具体的な Frame_Content_Size は指定されていないため、既定の辞書上限と等しい
1 GiB と仮定した。最初の標本は zstd CLI の既定 128 MiB 上限では拒否される一方、
`--memory=1024MB` では出力 0 バイト・終了 0 となり、KaitoKit の出力と一致した。
実装仕様からの逸脱はない。

ログは `.build/zstd-fix-check/` の `build.log`、`regression.log`、`path-check.log`、
`list-128k.log`、`list-text.log`、`full-test-final.log`、`timing.log`、`oracle.log` に保存した。
初回の URL 表記差による失敗は `full-test.log` に残した。
検証した実装・テストの SHA-256 は `tested-source.sha256` で最終状態との一致を確認し、
`git diff --check` は出力なし・終了 0。変更ファイルは指定範囲の 6 ファイルのみ。

### orchestrator による独立検証(是正後)

Codex の検証とは別に、orchestrator が同一 worktree から release / ASan の `kaito` を組み直して実施した。

**是正前後の直接比較。** 是正前は同じブランチの tip(`865246c`)を別 worktree に取り出して
release build したもの。同一入力・同一コマンドで比較した。

| 入力 | 是正前 | 是正後 |
|---|---|---|
| `28b52ffd00a0010000` × 10,000(90,000 バイト)を `kaito sha` | 42.69 秒 real / 42.34 秒 user、最大 RSS **1,085,652,992 バイト** | 0.00 秒 real、最大 RSS 11,763,712 バイト |
| 同じ入力を `kaito list` | — | 0.30 秒 real、最大 RSS 7,192,576 バイト |
| 16 バイトの 1 GiB 宣言単一フレーム | — | 0.00 秒 real、最大 RSS 11,681,792 バイト |

是正前の 1.09 GB という最大 RSS は、フレームごとの即時ゼロ確保が実在したことを直接示す。
是正後は同じ入力で 11 MB 台に収まる。

**遅延リングの折り返し経路。** この修正で最も危険なのは、確保量が保持上限に達して
リング運用へ移る境界である。宣言 window を入力よりはるかに小さくし、
Frame_Content_Size も落とした標本で参照実装と突き合わせた。

| 標本 | window | 出力 | kaito の SHA-256 | `zstd -dc` の SHA-256 |
|---|---:|---:|---|---|
| rfc8878.txt × 3、`--zstd=wlog=10 --no-content-size` | 1,024 | 337,275 | `6e0891fc…d5e8a2` | `6e0891fc…d5e8a2` |
| 同上 `wlog=11` | 2,048 | 337,275 | `6e0891fc…d5e8a2` | `6e0891fc…d5e8a2` |
| 同上 `wlog=14` | 16,384 | 337,275 | `6e0891fc…d5e8a2` | `6e0891fc…d5e8a2` |

1 KiB window で 337 KB を出力するので、リングは 330 周ちかく折り返す。

**差分オラクル。** `zstd` CLI を黒箱として比較する既存ハーネスに、
「小 window(wlog 10 / 11 / 12 / 14)+ `--no-content-size` + `--single-thread`」を
30% の確率で選ぶ経路を追加した(折り返しを実際に踏ませるため)。
seed 1 / 2 で各 80 ケース、計 160 ケースすべて一致、失敗 0。

**ASan での敵対的入力。** 同じ worktree から
`swift build --scratch-path .build-asan -Xswiftc -sanitize=address --product kaito` で組み、
既存の fixture 46 件に小 window・サイズ無しの生成標本 3 件を加えて、切り詰め・bit 反転・
frame header 各 field の総当り・過大宣言を流した。**13,818 実行で sanitizer 報告と crash は 0。**
window の index 計算が変わったため、是正前の ASan 結果は引き継がず全て取り直した。

**テスト。** worktree で `swift test` を実行し、**922 件(899 + 23)、38 skip、失敗 0、
`warning:` 0 件**。

差分ハーネス・変異ハーネス・ログは本セッションの一時領域にあり、セッション終了で失われる。
